# Create a Development Branch

Create a new `dev` branch, switch to it, and push the newly created branch.
For each step, create a separate branch. When you finish that step, create 
a pull request to the dev branch and merge it.